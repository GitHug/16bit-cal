package Control;

import Model.SixteenBitModel;
import javax.swing.JFrame;


/**
 * Creates the maincontroller which starts up everything
 * @author Group 6
 */
public class Main {
    public static Canvas canvas;


    /**
     * Starting point of the entire program
     * 
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        // TODO code application logic here 
        SixteenBitModel model = new SixteenBitModel();
        ComponentLoader cl = new ComponentLoader(model);
        //Modified model
        model = cl.getModel();
        canvas = new Canvas(model, cl.get());
        JFrame f = new JFrame("SimpleEvent");
        f.add(canvas);
        f.setSize(980, 620);
        f.setVisible(true);
        f.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
    
    }
}
